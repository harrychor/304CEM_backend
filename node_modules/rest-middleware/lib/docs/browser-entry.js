"use strict";

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var _react = require("react");

var _react2 = _interopRequireDefault(_react);

var _index = require("./index");

// Render the live React components over the
// statically server-rendered HTML.
var script = document.getElementById("manifestJson");
var manifest = JSON.parse(script.dataset.manifest);

_react2["default"].render(_react2["default"].createElement(_index.Shell, { manifest: manifest }), document.getElementById("page-root"));

// TEMP - TEST CODE
// ----------------------------------------------------------------------------
// import rest from "../../browser";
// let server = rest();
// server.onReady(() => {
//
//     console.log("server", server);
//     server.methods.foo.bar.put("my-id", "lorem").then((result) => {
//       console.log("result", result);
//     });
//
//     server.methods.bar.get("my-foo-get", 5).then((result) => {
//       console.log("result", result);
//     });
//
// });